APP="adapter"

BUILD_HOST="35.227.35.171"
BUILD_USER="sshilin"
BUILD_AT="/home/sshilin/adapter_build"

PRODUCTION_HOSTS="35.227.35.171"
PRODUCTION_USER="sshilin"
DELIVER_TO="/home/sshilin/adapter_release"

pre_erlang_get_and_update_deps() {
  local _prod_secret_path="/home/sshilin/prod.secret.exs"
  if [ "$TARGET_MIX_ENV" = "prod" ]; then
    __sync_remote "
      ln -sfn '$_prod_secret_path' '$BUILD_AT/config/prod.secret.exs'
    "
  fi
}